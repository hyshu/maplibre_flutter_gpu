# maplibre_gl visual reference fixture

Android and iOS reference fixture used to compare visual output with
`maplibre_flutter_gpu`. Run it through the parent E2E runner. This is not a
standalone product example.
